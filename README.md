# Ayush Dev — Portfolio

An editorial personal portfolio and journal built with Astro. The site is statically generated and deployed on Vercel.

## Development

```bash
npm install
npm run dev
```

Run `npm run build` before publishing. Journal entries live in `src/content/journal`; drafts are excluded from pages and RSS when `draft: true`.
